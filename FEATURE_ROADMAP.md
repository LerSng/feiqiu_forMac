# 功能盘点与后续路线规划

整理日期：2026-09-09 · 当前代码版本：0.1.1（Build 2）

> 本文基于当前源码、README 和现有检查程序做静态梳理，不是一次新的 Windows 双机验收，也不是逐行安全审计。除“当前已实现”外，下文均为建议，不能视为已经支持或承诺交付。

## 结论先看

当前项目已经不只是“能收发消息”的客户端，历史管理、附件管理和本地维护底座比较完整。下一阶段最值得投入的是：**不丢草稿、明确消息是否送达、任务中断可恢复，以及把聊天资料变成容易复用的信息。**

建议把后续能力分成三层，而不是简单地“兼容 Windows / 不兼容 Windows”：

| 路线 | 谁需要升级 | 适合做什么 | 是否需要服务器 |
| --- | --- | --- | --- |
| A：本机独占增强 | 只升级这台 Mac | 草稿、收藏、OCR、截图标注、快捷操作、隐私管理 | 通常不需要 |
| B：增强客户端互通 | 通信双方使用本项目或同协议的新客户端 | 安全配对、可靠消息、真正群聊、原图、引用、续传 | 同一局域网内可以不需要 |
| C：配套服务扩展 | 客户端及配套服务一起建设 | 离线收件、跨公网、多设备同步、团队管理 | 通常需要在线服务节点 |

**最推荐：先做 A，再按实际需求做 B；C 暂时不要作为默认前提。** 只做 A 就能拥有很多旧飞秋没有的好用功能，同时继续给 Windows 飞秋发送普通消息。

如果决定完全不对接 Windows 飞秋，可以停用旧协议入口，直接建设 B；这不要求推倒现有界面、SQLite、历史搜索或附件管理。

## 1. 当前已实现

以下能力已有实现，不应再作为“从零新增”的功能重复安排：

| 模块 | 已有能力 | 仍需注意 |
| --- | --- | --- |
| 基础聊天 | 局域网发现、在线状态、文字、图片、普通文件、输入状态、表情、抖一抖 | 旧协议没有端到端加密；网络回执不等于界面已读 |
| 历史记录 | 联系人 / 群聊、日期、关键字、文本 / 图片 / 文件筛选，分页、定位原消息 | 关键字搜索正文和文件名，不搜索图片文字或文件正文 |
| 记录归档 | TXT、Markdown、HTML 导出及附件副本，本应用完整导出记录的合并导入 | 不等于支持任意第三方 TXT；导入不同于覆盖恢复 |
| 图片体验 | 缩略图、多图卡片、会话图库、左右卡片切换、缩放、旋转、复制 | Windows 图片仍需实际失败样本及双机验证 |
| 附件发送 | 选择文件、粘贴图片、系统截屏、拖拽直发；文件图标显示后缀 | 暂不支持目录；图片路径默认转成 JPEG |
| 会话管理 | 置顶、免打扰、备注、标签、屏蔽、未读及联系人搜索 | 会话置顶不是消息收藏，联系人备注不是消息笔记 |
| 下载中心 | 接收任务、跨会话历史附件、全部传输、进度、队列暂停、排序、取消、重试 | 历史附件跨重启；当前传输任务列表不跨重启 |
| 身份与提醒 | IP 变化延续可确认的同一设备会话；提示音选择、试听、静音 | 设备匹配不是密码学身份认证；已有提示音选择无需重复建设 |
| 数据维护 | 占用统计、旧附件清理预览、完整备份、校验恢复、安全备份 | 数据及备份未加密；恢复后要求重启 |
| 群聊与协助 | Mac 中继群、远程协助请求识别 | 不是真正群协议；没有画面传输或键鼠控制 |

现有自动检查主要覆盖临时数据、模拟协议和本机 Socket。真实 DHCP、多设备收发、通知声音、GUI 手势及签名发行仍需单独验收。

## 2. 最值得优先补的短板

| 当前代码表现 | 对使用的影响 | 建议方向 |
| --- | --- | --- |
| 切换联系人 / 群聊会清空 `draft` 和草稿附件 | 写到一半切会话，内容无法保留 | 会话级草稿、自动保存、重启恢复 |
| `ChatMessage` 没有投递状态；普通消息回执分支未驱动消息状态 | 消息出现在自己的聊天记录中，不代表对方收到 | 包号关联、持久化待发送箱、明确确认 / 未确认 / 失败 |
| `FileTransferCenter` 的任务和执行闭包保存在内存 | 退出后无法继续管理原任务 | 保存可恢复任务描述，重启后重新检查并构建任务 |
| 下载请求的偏移量为 `0`；发送端虽然支持 offset | 当前失败重试仍从头开始 | 区分任务恢复和断点续传，不能把二者混为一谈 |
| 搜索使用正文 / 文件名的 `LIKE` 查询 | 图片内容搜不到，大历史量的检索能力还有提升空间 | 图片 OCR、文件内容抽取、专用全文索引 |
| 中继群会转发成员发给 Mac 的普通私聊 | 存在私聊意外进入群的隐私风险 | 先强化风险提示和启用控制；根治需要明确群 ID 的新协议 |
| 图片发送统一转 JPEG，GIF 仅首帧，透明度不保留 | 设计图、动图和原始素材不能完整保真 | 增加压缩 / 原文件发送选择；增强协议直接保留媒体原格式 |
| 旧通信、本地数据库和备份未加密 | 不适合直接按“安全聊天工具”对外宣传 | 分别设计传输认证、端到端加密及本地静态加密 |

这些判断对应的源码入口见文末。已有的 IP 变化适配、传输重试、缩略图、维护清理应继续复用，而不是再建一套平行实现。

## 3. 路线 A：只改 Mac 就能做的独占功能

### 3.1 功能清单

优先级：**P0** 为可靠性 / 安全前置，**P1** 为高频高价值，**P2** 为按需求扩展。复杂度为相对范围：S 小、M 中、L 大、XL 独立子项目，包含必要的数据迁移和验证，不代表工期。

| 编号 | 建议功能 | 具体体验 | 优先级 / 复杂度 | Windows 兼容边界 |
| --- | --- | --- | --- | --- |
| A01 | 会话草稿自动保存 | 每个联系人 / 群聊独立保存文字、附件和编辑状态，重启可恢复 | P0 / M | 完全本地，不改变对端 |
| A02 | 消息状态与待发送箱 | 显示等待、发送中、协议确认、未确认、失败；失败可重试 | P0 / L | 只能展示旧端实际可确认的状态，不能伪造已读 |
| A03 | 中继风险与接收策略 | 中继显著标识、启用确认；陌生设备消息请求、大附件接收确认、自动下载白名单 | P0 / M–L | 本机策略；提示不能根治中继私聊混流 |
| A04 | 传输任务持久化 | 重启仍能看到任务状态、错误和恢复入口，自动使用已确认联系人的最新地址 | P1 / L | 对端文件任务可能已失效；恢复不保证续传 |
| A05 | 消息收藏与个人笔记 | 收藏文本 / 图片 / 文件，按项目归档，添加只对自己可见的备注和提醒 | P1 / M | 不发送收藏、笔记或提醒状态给对方 |
| A06 | 图片 OCR 与文件正文搜索 | 搜截图中的报错、订单号；提取 PDF 正文，结果跳转原消息 | P1 / L | 本地索引，不要求对端支持 |
| A07 | 截图标注与隐私处理 | 截屏后画箭头、编号、裁剪、遮盖敏感信息，预览确认后发送 | P1 / M | 导出常规图片即可；敏感信息用真正覆盖像素的打码 |
| A08 | 引用、转发与代码消息 | 引用原消息、批量转发、代码块高亮、本地 Markdown 展示 | P1 / M–L | 给旧端发送可读文本 / 普通附件，不保证可点击引用和样式一致 |
| A09 | 更好用的附件工具 | 发送前选压缩或原文件、批量保存、相同内容复用、图片和文件分组整理 | P1 / M–L | 原图可作为普通文件发送；旧端不保证内嵌预览 |
| A10 | 网络诊断与自动恢复 | 睡眠唤醒后重建连接、网络变化重发现、多网卡选择、手动地址、脱敏诊断包 | P1 / L | 不能穿透防火墙、VLAN 或 NAT；需真实网络验收 |
| A11 | Mac 系统快捷入口 | 菜单栏快速发送、全局快捷键、Finder 分享、Services / Shortcuts、通知快捷回复 | P1 / L | 分步接入；扩展权限、签名和后台生命周期需验证 |
| A12 | 通知隐私与精细提醒 | 锁屏隐藏正文、分联系人提示音、自定义音效、重要消息例外 | P1 / M | 不绕过系统通知权限和专注模式 |
| A13 | 自动备份与恢复向导 | 备份轮换、保留数量、失败提醒、空间不足保护、明确区分合并与覆盖 | P1 / M–L | 本地定时任务受应用运行 / 睡眠状态影响 |
| A14 | 应用锁与加密备份 | Touch ID / 密码解锁、加密归档、明确的密钥备份和恢复流程 | P1 / L | 应用锁不是磁盘加密；数据库 / 附件加密另行评估 |
| A15 | 本地智能助手 | 对选中会话或日期范围做摘要、提取待办、翻译、总结文件 | P2 / L | 默认本地处理；使用云模型前明确授权发送的数据范围 |
| A16 | 个性化工作台 | 保存搜索条件、智能会话分组、快捷短语、字体与对比度、键盘和 VoiceOver 操作 | P2 / M–L | 智能分组应复用已有标签，不另造重复联系人体系 |

### 3.2 最有辨识度的本机体验

**“聊天也是资料库”**：搜索不只找正文，还能找图片中的文字；把结果收藏到项目下，再补个人笔记。收到的资料不需要反复翻会话寻找。

**“截图即说明”**：截屏 → 标注箭头 / 步骤 → 覆盖敏感区域 → 确认发送。现有系统截屏和图片发送已具备，重点补编辑环节，不必重做截图入口。

**“随时切换，不丢工作”**：写一半切去回复别人，再回来草稿仍在；发送失败和文件中断有统一的恢复入口。

**“Mac 随手发”**：选中文件从 Finder 分享，或用快捷键打开快速发送面板。必须展示目标联系人并由用户确认，不默认把剪贴板后台广播出去。

这些体验不需要 Windows 飞秋实现同样界面，也不需要先建设云端账号体系。

### 3.3 开发时容易遗漏的边界

- **草稿附件**：必须持有可恢复的受管理副本或有效访问凭据；只保存临时路径，重启后仍可能丢失。切换、成功发送、取消、屏蔽、恢复数据库时都要明确行为。
- **收藏与清理**：明确“仅收藏消息引用”还是“保留附件副本”。消息删除、旧附件清理时不能悄悄破坏用户选择保留的收藏，也不能让收藏意外阻止所有清理。
- **OCR 与全文索引**：先做图片和 PDF，后续再按需要接入 DOCX 等格式。验证中文分词、扫描 PDF、分页命中和索引重建；不能直接认定默认 FTS 分词就适合中文。
- **索引隐私**：索引也是聊天数据；删除消息或附件后同步清理可检索内容。索引建议可重建，恢复备份后不要使用过期内容。Spotlight 集成应默认关闭、可撤销。
- **引用 / 转发**：本地引用可定位原消息，旧端只收到文本说明。转发前展示目标和包含的内容，避免误带私人备注或原会话身份信息。
- **提醒与定时发送**：可以做，但应区分“本机运行时到点处理”和“保证按时送达”。Mac 休眠、退出或双方离线时，不能承诺后者。
- **本地 AI**：需评估模型体积、内存、速度与硬件支持；摘要附原消息来源，待办由用户确认，不自动发送消息或执行附件里的指令。

## 4. 路线 B：不受 Windows 飞秋协议限制

前提是**双方都使用支持新协议的客户端**。可以先只实现 Mac ↔ Mac，再考虑自研 Windows 客户端；“不用对接旧 Windows 飞秋”不等于“不能服务 Windows 用户”。

### 4.1 能力清单

下表优先级在选择这条路线后生效；不是要求与路线 A 同时启动全部项目。

| 编号 | 建议功能 | 可以获得的能力 | 前提与边界 | 优先级 / 复杂度 |
| --- | --- | --- | --- | --- |
| B01 | 版本协商与可信设备配对 | 二维码 / 短码配对、设备名片、密钥指纹确认、可信设备列表 | 发现设备不代表信任；不能仅凭昵称、IP 或自报设备 ID 认证 | P0 / L |
| B02 | 可靠消息协议 | 稳定消息 ID、持久化待发送箱、确认、重试、跨重启去重、状态同步 | 先定义确认语义；不是仅把 UDP 换成 TCP 就完成可靠投递 | P0 / L |
| B03 | 加密与密钥管理 | 经认证的安全连接，按威胁模型设计端到端加密 | 用成熟协议 / 库，处理配对撤销、换机、密钥恢复及群密钥更新 | P0 / XL |
| B04 | 真正的原生群聊 | 群 ID、邀请接受 / 拒绝、成员同步、管理员、退出 / 移除、群公告 | 私聊绝不自动进群；先明确群消息分发拓扑和离线规则 | P1 / XL |
| B05 | 送达和已读分离 | 对端接收状态、可选阅读回执、群成员阅读情况 | 已读需用户设置和隐私控制；不把协议确认当人已阅读 | P1 / L |
| B06 | 结构化聊天交互 | 可定位回复、编辑、撤回、表情回应、线程讨论、@提及 | 双端状态和历史版本要一致；撤回无法消除截图、导出或旧备份 | P1 / L |
| B07 | 原生媒体消息 | 原图、透明 PNG、动图、视频预览、语音消息及可选转写 | 不再强制转 JPEG；仍需尺寸 / 时长限制、解码安全和缓存策略 | P1 / L |
| B08 | 真正的文件续传 | 文件清单、分块、校验、断点协商、重启继续、失败块重发 | 校验源文件未变化，防止拼接错内容；大文件限制需重新设计和测试 | P1 / L–XL |
| B09 | 文件夹传输 | 保留目录结构、选择接收项、目录级进度与冲突处理 | 防路径穿越、符号链接、覆盖和超额展开；不是直接信任远端路径 | P1 / L |
| B10 | 在线设备接力 | 已配对设备间发送剪贴板、图片、文件和临时笔记 | 先做用户主动发起的一次性传送，不默认同步密码等敏感内容 | P2 / L |
| B11 | 轻量协作 | 投票、共享待办、消息转任务、群内事项确认 | 需要操作权限、版本与冲突规则，避免变成另一个完整项目管理系统 | P2 / L |
| B12 | 音视频 / 屏幕协助 | 语音通话、屏幕共享，后续可评估明确授权的远程控制 | 独立大项目；需要权限、会话认证、实时媒体和随时终止机制 | P2 / XL |

### 4.2 不再受旧协议牵制后，可以简化什么

- 统一 UTF-8 和版本化消息结构，不再依赖旧客户端私有富文本及表情标记。
- 图片走统一附件通道，不再为新客户端复制 Windows 私有 DIB、UDP 分片和图片 ID 拼装路径。
- 协议中明确区分 `ConversationID`、`GroupID`、`MessageID` 和设备身份，不再靠群名前缀猜测路由。
- 返回结构化错误和可恢复状态，不再把“已写入本地历史”当作发送成功。
- 用能力协商解释“对方不支持”，而不是尝试发送后让对方看到乱码或无效附件。

但新协议仍然要解决包丢失、重复、进程退出、版本差异、资源限制和身份冒充。摆脱旧协议只是减少兼容负担，不会自动解决这些问题。

### 4.3 群聊应优先解决的不是外观

1. 普通私聊和群消息有不同的明确路由，绝不因“发送者属于某个群”就进行中继。
2. 邀请有待响应、接受、拒绝、过期、撤销状态；未接受的人不是正式成员。
3. 群成员变化有版本和权限检查，重复或乱序操作不能让已退出成员重新出现。
4. 先选择小规模在线群的分发方式；如果采用群主设备中继，要明确其离线时的行为。
5. 历史中继群保留原标识和历史，不自动变成“所有成员已经同意加入”的新群。

现有 [GROUP_INTEROP.md](GROUP_INTEROP.md) 可复用其邀请与路由边界，但其中早期“后续必要功能建议”有部分现已完成，应以当前代码及本文盘点为准。

## 5. 路线 C：需要配套服务或额外平台的能力

### 5.1 配套服务

| 目标 | 除客户端外还需要什么 | 不能省略的限制 |
| --- | --- | --- |
| 离线消息 / 离线文件 | 可持续在线的存储转发节点、保留策略、配额和确认机制 | 双方都离线且没有在线节点时，纯 P2P 不能保证收件；本地排队不等于离线送达 |
| 跨公网聊天 | 地址发现 / 信令、连接协商，必要时 NAT 穿透和中继 | 同一局域网发现不能直接变成公网连接；可评估现成网络方案而非从零造一套 |
| 多设备历史同步 | 增量变更日志、冲突规则、删除传播、附件同步、设备及密钥管理 | 在线设备可先做点对点同步；异步可靠同步需要在线节点，导入导出不能替代同步协议 |
| 团队目录与权限 | 可管理的身份目录、邀请 / 禁用、组织权限、可运维的服务 | 管理审计与端到端加密需明确边界，不能宣称管理员能看全部内容又对管理员端到端保密 |
| Webhook / 机器人 | 经过授权的事件接口、凭据存储、限流与运行节点 | 默认不给机器人全历史权限；外发内容、目标和执行记录要可查 |
| 手机离线提醒 | 移动端应用、平台推送和配套后端 | iOS 不能像桌面程序一样承诺后台长期监听局域网 Socket |

服务可以是局域网常开设备或自托管服务，不一定是公有云；但仍需维护升级、备份、安全和可用性。

### 5.2 新平台不是“顺便编译一下”

- **自研 Windows 客户端**：可以共享新协议和测试用例，但当前 SwiftUI / AppKit 界面不能直接运行 Windows。需单独评估 UI 技术和可共享的数据 / 网络层。
- **iPhone / iPad 客户端**：可以复用部分 Swift 模型和逻辑，仍需处理后台、推送、本地网络权限和文件访问差异。
- **浏览器临时收发**：可以作为独立模式评估，但需要网页入口、受保护的连接和限时配对，不能把整个聊天数据库作为公开 Web 服务暴露。

推荐顺序是：**两台 Mac 的新协议闭环 → 根据真实用户需求选一个新平台 → 再决定是否建设服务。**

## 6. 架构怎样演进，才能保留现有投入

### 6.1 复用业务层，隔离协议层

当前已有 View / ViewModel / Repository / Service / Persistence 分层，以及发现、消息、文件等网络接口，可以沿这个方向演进，不必立刻重写所有界面。

以下是建议的逻辑结构，名称不代表仓库已经存在这些类型：

```text
聊天界面 / 发送与接收用例 / 历史与附件管理
                    ↓
统一消息模型 + 本地待发送箱 + 可信设备映射
                    ↓
路由与能力策略（已认证的能力协商、安全要求、失败反馈）
          ├── LegacyFeiQAdapter：现有飞秋协议
          └── EnhancedTransport：新客户端协议
                    ↓
SQLite / 受管理附件 / 索引 / 密钥存储
```

- 兼容路线保留旧适配器；纯增强版本可以关闭旧适配器，但保留旧历史的读取和导入。
- 底层事件逐步转换成与协议无关的消息 / 回执 / 附件事件，避免每个新功能都让界面解析 `FeiQPacket`。
- 新客户端先认证设备，再协商能力。未经认证的广播只用于发现候选设备，不用于建立信任。
- 对方不支持某功能时显式禁用或解释降级。**要求加密的消息不能静默退回旧飞秋明文。**
- 现有设备 ID 主要用于会话关联，并非可信公钥身份；升级时经用户确认绑定，不按同名自动合并不同设备。

### 6.2 新数据必须走完整生命周期

增加草稿、待发送箱、收藏、消息版本或 OCR 索引时，不能只加一个表和一个界面：

1. 定义数据版本与 SQLite 迁移，保证旧库和旧归档仍可读取。
2. 明确每类数据是否参与导出 / 导入、完整备份、恢复和去重；同步维护备份允许的表结构与校验逻辑。
3. 草稿、待发送任务、收藏引用与附件清理保护相互配合；恢复数据库后重新验证任务，不立即自动补发旧消息。
4. 文件索引和缩略图可重建，删除后的内容不能仍通过缓存或全文索引查到。
5. 重启恢复保存的是任务参数和状态，不是序列化执行闭包；源文件、权限、设备身份和远端任务有效性要重新检查。
6. 新增临时数据库和模拟网络回归，不用真实聊天记录测试清理或恢复。

### 6.3 安全能力分三层，不要混称

| 层次 | 解决什么 | 没有自动解决什么 |
| --- | --- | --- |
| 经认证的传输加密，例如 TLS | 保护连接，验证连接另一端的身份 | 经服务器终止的 TLS 不等于服务器无法读消息 |
| 端到端加密 | 让中继 / 存储服务无法读取消息内容 | 不保护已解锁终端、截图、人工转发或明文导出 |
| 本地数据库 / 附件 / 备份加密 | 降低存储介质或备份泄露的风险 | 不等于网络加密，也不等于仅加一个应用锁 |

密钥放置、丢失恢复、设备撤销和新成员加入时的密钥更新应先设计，再宣传加密功能；优先评估成熟实现，不自行发明密码学协议。

## 7. 推荐开发顺序与验收目标

### 阶段一：让现有客户端更可靠

- 会话级草稿、消息确认状态、待发送箱、任务跨重启恢复。
- 中继群风险控制、陌生设备和自动下载策略。
- 整理签名 / DMG 脚本，补真实 Windows 收发和网络变化验收。

**验收重点**：切换会话和重启不丢草稿；未确认的消息不显示成已送达；离线排队不会串联系人；无法恢复的任务明确提示重发；既有 Windows 收发和历史数据不退化。

### 阶段二：做出本机独占的效率优势

- 收藏与笔记、图片 OCR、PDF 正文检索、截图标注。
- 原文件发送选项、批量整理附件、快捷发送入口和通知隐私。

**验收重点**：能够搜索截图里的中文并定位原消息；原图不被悄悄转码；打码后不能从发送文件恢复敏感像素；清理 / 恢复与收藏、索引保持一致。

### 阶段三：验证无服务器增强通信 MVP

限定为两台 Mac：可信配对、版本协商、安全连接、可靠文字消息、原图 / 普通文件传输，复用既有历史和下载中心。

**验收重点**：同名设备不误认，IP 变化不破坏已确认身份；重试和重启不重复显示同一消息；不支持的版本有明确提示；断开 / 撤销配对后不能继续凭旧信任发送；安全要求不静默降级。

此阶段不同时加入云账号、多人音视频、复杂同步和远程控制，先证明协议及数据生命周期可靠。

### 阶段四：扩展真正群聊与新交互

- 原生邀请与成员权限、明确群 ID、回复 / 编辑 / 回应、可选已读。
- 协商式文件断点续传和目录传输。

**验收重点**：三人群互通且私聊绝不进入群；拒绝邀请和非成员消息正确处理；同名群不串消息；乱序成员更新不回滚权限；中断后续传文件校验一致，源文件变化能够拒绝续传。

### 阶段五：按实际需求扩平台和服务

先选最有需求的一项：自研 Windows、移动端、跨公网或离线收件，再补对应的服务、同步和运维能力。不要一次把所有方向都变成首发条件。

## 8. 近期不建议优先投入

- **为了表面兼容继续猜 Windows 私有群聊 / 语音 / 远控协议**：没有授权样本和明确验收目标时，投入大、结果不确定；可直接走增强协议路线。
- **只做大量气泡皮肤和动画**：已有图片卡片体验，优先级应低于不丢草稿、发送反馈和可恢复任务。
- **先搭完整云账号 / 团队后台**：当前主要是局域网桌面工具，没有真实需求前会显著增加维护和隐私负担。
- **不做能力协商就加入撤回、已读和群邀请**：只改本机界面无法让旧 Windows 客户端获得对应语义。
- **直接把历史数据库当作多端同步文件**：并发写入、附件一致性、删除传播和身份合并都需要协议，不能靠目录同步代替。
- **自动打开收到的附件或默认执行机器人指令**：先做确认、权限与审计，避免快捷功能变成新的风险入口。

## 9. 源码对照与开工入口

这些入口用于后续设计定位，行号对应本次梳理时的代码，后续可能变化：

| 主题 | 代码 / 文档入口 | 观察到的事实 |
| --- | --- | --- |
| 草稿与会话切换 | [ChatViewModel.swift:1508](FeiQMac/ViewModels/ChatViewModel.swift#L1508) | `activateConversation` 清空文字和附件草稿 |
| 消息数据模型 | [ChatMessage.swift:194](FeiQMac/Models/ChatMessage.swift#L194) | 有正文、时间、附件，无投递 / 引用 / 编辑版本字段 |
| 消息回执 | [ChatRepository.swift:1348](FeiQMac/Repositories/ChatRepository.swift#L1348) | 普通消息确认、阅读等命令分支未驱动消息状态 |
| 中继群路由 | [ChatRepository.swift:1497](FeiQMac/Repositories/ChatRepository.swift#L1497) | 按发送者所属群处理及中继普通私聊 |
| 传输调度 | [FileTransferCenter.swift:25](FeiQMac/Services/FileTransferCenter.swift#L25) | 内存保存任务、顺序及执行闭包 |
| 接收端偏移 | [FeiQNetworkService.swift:629](FeiQMac/Network/FeiQNetworkService.swift#L629) | 文件下载请求从偏移 `0` 开始 |
| 历史检索 | [ChatHistoryStore.swift:1174](FeiQMac/Persistence/ChatHistoryStore.swift#L1174) | 正文与附件名使用 `LIKE` 条件 |
| 网络抽象 | [NetworkServices.swift:9](FeiQMac/Network/NetworkServices.swift#L9) | 已有发现、消息和文件等网络服务接口，可继续解耦 |
| 当前能力与验证 | [README.md](README.md)、[Tests/](Tests/) | 已实现边界、检查入口及待完成实机验收 |
| 群协议设计边界 | [GROUP_INTEROP.md](GROUP_INTEROP.md) | 邀请、明确群路由、成员权限和验收场景 |

**最终建议：先做“可靠的 Mac 聊天资料工作台”，再做“双方自愿升级的增强聊天协议”。** 前者能快速改善每天的使用，后者才是摆脱旧飞秋限制、实现真正群聊和安全通信的正确入口。
