# 原生群聊实现前置条件与验收范围

## 当前状态

目前项目的群聊是 Mac 中继，并非 Windows 飞秋 2013 原生群协议。仓库中的 feiqiu-README.md 第 8 行明确说明，群号、成员管理、邀请、申请入群和群消息属于尚未解析的私有协议。

已有中继还有一个重要限制：它会将已配置成员发来的普通私聊转发给同群其他成员，不能可靠区分私聊与群聊。请勿将敏感私聊联系人加入这种中继群。新方案必须使用明确的群标识路由，不能继续用“发送人属于某个群”来猜测消息归属。

本轮未添加虚构的 Windows 群命令，也未把本地选择联系人当成对方接受邀请。

## 需要先确认的互通目标

1. **Windows 飞秋 2013 原生群互通**：需要对应版本的群操作抓包及操作时间记录，确认协议后实现。
2. **Mac 客户端之间的群聊**：可以定义带版本号的扩展协议，实现邀请、接受/拒绝、成员同步和群消息；这不等于兼容 Windows 原生群，不能向旧 Windows 客户端假装邀请成功。

这两种方案的发现、邀请和成员同步报文不同，需要先选择目标。

## Windows 原生群所需样本

使用两台有权限操作的 Windows 测试机器，先建立不含真实聊天记录的测试群，记录双方飞秋完整版本号、IP、操作时间和角色。建议每项操作单独保存 pcapng，包含请求、响应和重试。

| 场景 | 需要观察的结果 |
| --- | --- |
| 创建群、搜索群 | 群 ID、群主身份、群资料及发现方式 |
| 邀请未入群用户 | 邀请 ID、发起人、接收端弹窗 |
| 接受邀请、拒绝邀请 | 响应报文及群主端成员列表变化 |
| 邀请失效、重复邀请 | 有效期、去重和错误响应 |
| A 发消息，B 回复 | 消息中的群 ID、发送者、消息 ID、回执和分发路径 |
| 第三人加入 | 已有成员怎样获知成员变化 |
| 退出、移除成员、解散群 | 权限验证、成员列表变化和状态收敛 |
| 重启、离线后恢复 | 成员关系恢复、版本同步和遗漏消息处理 |

先观察相关测试机器的通信，不要预先认定群协议只使用 TCP/UDP 2425；它可能还包含其他端口或组播。只捕获有授权的测试通信。抓包可能包含明文消息、昵称及密码，提交前清除无关流量；测试密码不要复用真实密码。

## 实现架构

- **View**：建群向导、群资料、成员列表、邀请收件箱、接受/拒绝交互。
- **ViewModel**：界面状态、操作反馈、未读和通知跳转。
- **Repository**：群权限、邀请状态机、成员同步、去重、离线处理和数据组合。
- **Service**：协议报文收发、重试与持久化，不决定业务上的入群权限。
- **Model**：稳定的 GroupID、MemberID、InvitationID、MessageID，以及群角色和状态。

邀请流程应为“待响应 → 接受 / 拒绝 / 过期 / 撤销”；发送邀请不立即增加正式成员。只有经过确认的成员能发送群消息。来源校验不能仅信任正文中的用户名、群主标记或 IP 地址。

群、成员、邀请和消息应分类落库；成员更新需要版本或序号，重复包不能重复加人或重复显示消息。旧中继群数据须保留并明确标识，不自动迁移成对方已加入的新群。

## 必须通过的验收

- 三个成员都能看到同一群中的互相回复，普通私聊绝不进入群。
- 被邀请方可以接受或拒绝；拒绝、未响应的人不会成为正式成员。
- 两个同名群不串消息，联系人改昵称不改变身份。
- 重复邀请、重复消息、乱序成员更新不会产生重复记录或回滚成员状态。
- 非成员和已退出成员的群消息被拒绝；普通成员不能执行群主操作。
- 邀请、成员和历史记录可在重启后恢复；离线恢复和失败有明确提示。
- 图片按群消息 ID 关联；慢速、乱序和部分接收失败不丢失成功内容。

## 后续必要功能建议

群协议确定后，优先做可靠的发送状态及重试、分页历史搜索、文件传输列表和取消、会话免打扰、联系人屏蔽。以上是后续范围建议，不代表已经实现。
