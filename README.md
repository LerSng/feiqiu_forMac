# 飞秋 Mac

基于 SwiftUI 的 macOS 局域网即时通讯客户端，面向飞秋 2013 Windows 版提供用户发现、在线状态同步和文字消息互通能力。

项目采用分层架构，将界面状态、业务编排、协议通信、数据持久化和系统通知分别隔离，便于持续扩展协议兼容能力和聊天功能。

> 当前版本：`0.1.1`（Build `2`）

## 项目概览

| 项目 | 说明 |
| --- | --- |
| 支持平台 | macOS 14.0 及以上 |
| 用户界面 | SwiftUI |
| 开发语言 | Swift 5 |
| 网络协议 | 飞秋/IP Messenger 兼容报文 |
| 通信端口 | UDP/TCP `2425` |
| 本地存储 | SQLite |
| 兼容目标 | 飞秋 2013 Windows 版的局域网文字、图片与普通文件通信 |
| 数据范围 | 仅局域网直连，不依赖云端服务器 |

## 功能特性

### 局域网通信

- 通过 UDP `2425` 广播上线、回复上线、刷新用户列表和发送离线通知。
- 支持点对点文字消息及消息接收回执。
- 支持飞秋内嵌图片：正文图片标记 + UDP `0x2000C0` 分片，逐片回复 `0xC1`；发送端支持回执确认和有限重试。
- 支持标准 IPMsg/飞秋普通文件附件：UDP 通知文件元数据，TCP `2425` 流式传输文件内容。
- 支持聊天窗口内点击紧凑文件卡片使用 macOS Quick Look 预览，也可使用默认应用打开或在 Finder 中定位。
- 支持在消息输入框使用 `⌘V` 粘贴文本或图片；粘贴的图片会先进入待发送区，可移除后再发送。
- 支持使用输入区的截屏按钮调用 macOS 原生区域选择；截屏完成后自动回填到待发送区，确认后即可发送。
- 聊天图片支持右键“复制图片”，复制后可直接回到输入框粘贴发送。
- 支持飞秋扩展版本头 `1_lbt6_0#...`，可被飞秋 2013 Windows 版识别。
- 支持 GB18030/GBK 兼容编码，降低中文消息在 Windows 版中出现乱码的概率。
- 支持 IPv4 局域网内的 UDP 消息接收和 TCP `2425` 监听。

### 聊天体验

- 联系人搜索、在线状态、最近会话和未读消息数量展示。
- 消息使用自适应宽度气泡显示，区分发送方和接收方。
- 窗口标题栏跟随当前联系人或群聊，应用品牌固定在侧边栏顶部；联系人状态和操作保持在统一的轻量会话栏中。
- 支持消息时间展示，点击日期可查看完整的年月日、时分秒。
- 支持消息进入动画，包括“两侧汇聚”、淡入、上浮、缩放、立即显示和随机模式。
- 支持通过消息右键菜单删除聊天内容；删除会同步持久化、清理未被其他消息引用的本地附件，并使用淡出缩放动画更新列表。
- 支持 macOS 系统通知；点击通知后可直接进入对应联系人或群聊会话。
- 支持 96 项飞秋内置表情码双向转换；表情面板只展示有协议映射的表情。
  - Mac 使用含义近似的 Unicode emoji，Windows 收到飞秋原生表情码，例如 `/:)` ↔ `🙂`、`/:D` ↔ `😁`、`/:love` ↔ `❤️`。
  - 兼容连续表情、正文混排、字体标记，保留旧版 `/:fd`、`/:o` 别名解析。
  - 支持已映射 emoji 的文本/彩色变体及肤色变体；飞秋原生表情不保留肤色差异。
- 单聊顶部提供“抖一抖”：发送或收到请求时触发窗口动画并记录会话提示；发送间隔为 3 秒，同一来源的短时间重复请求不会反复抖动。
- 抖动不会强制切换当前会话，后台接收时更新未读及通知；尊重系统“减少动态效果”，全屏使用窗口内容动画。
- 各模块永久隐藏滚动条，保留鼠标滚轮、触控板及输入框光标跟随滚动。
- 图片统一保存到 `~/Documents/飞秋 Mac/Images`，普通文件保存到 `~/Documents/飞秋 Mac/Files`；聊天记录只保存附件元数据和本地路径。
- 同一条消息最多合并 9 张照片，以折叠卡片展示；支持左右按钮、横向滑动翻页、当前页预览和单张删除。

### 群聊

- 支持在 Mac 端创建、编辑和删除群聊。
- 支持从在线联系人中选择群成员，并持久化群聊成员关系。
- Mac 作为群聊中继节点：Mac 发送的群消息会转发给所有在线成员，成员发送给 Mac 的消息也会中继给同群其他在线成员。
- Windows 成员收到中继消息时，会在普通飞秋消息中看到 `【飞秋群聊：群名】发送人：消息` 标记，从而识别群聊名称和原始发送人。
- 群聊消息、未读数量和群聊配置均纳入统一的本地历史记录体系。

### 历史记录与性能

- 聊天记录统一保存到 SQLite，不因重新编译或重新启动应用而丢失。
- 联系人、群聊、成员关系、会话摘要和消息记录分类存储。
- 数据库访问使用后台串行队列，避免阻塞 SwiftUI 主线程。
- 会话首次进入只加载最近一页消息，向上滚动时再分页加载更早记录。
- 支持从早期版本的 `ChatHistory.json` 迁移到 SQLite。
- 提供网络诊断日志，便于定位端口、编码、广播和回执问题。

## 协议兼容范围

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| 用户发现 | 支持 | UDP 广播上线、回复上线、刷新列表及离线状态 |
| 在线信息 | 支持 | 解析飞秋昵称、主机名和分组信息 |
| 文字消息 | 支持 | 使用 UDP `2425` 发送和接收点对点消息 |
| 消息回执 | 支持 | 发送接收回执并记录网络状态 |
| 中文编码 | 支持 | 优先兼容飞秋 Windows 使用的 GB18030/GBK 编码 |
| UTF-8 报文 | 部分支持 | 根据报文选项识别 UTF-8，旧版飞秋消息仍按传统编码处理 |
| 飞秋扩展版本 | 支持 | 兼容 `1_lbt6_0#...` 版本标识和部分私有上线命令 |
| 飞秋表情 | 已实现，待 Windows 双机验证 | 覆盖参考码表中的 96 项；Mac 使用近似 emoji，不复刻 Windows GIF |
| 窗口抖动 | 已实现，待 Windows 双机验证 | UDP `0xD1` 请求、`0xD2` 回执；回执不会触发再次抖动 |
| 远程协助请求 | 已识别，控制通道待协议验证 | 识别飞秋私有 UDP `0xB0`，去重并弹出安全确认卡片；暂不自动开放屏幕、键盘或鼠标 |
| TCP 数据连接 | 支持 | 普通文件附件通过 TCP `2425` 请求下载，支持按消息包号和文件 ID 匹配 |
| 内嵌图片 | 已实现，待 Windows 双机验证 | 关联正文图片 ID，UDP 分片重组、逐片确认、去重和有限重试 |
| 多图折叠卡片 | 支持 | 一条消息最多 9 张图片，Mac 端以折叠卡片和左右翻页展示；Windows 端仍按飞秋原生图片协议接收 |
| 普通文件附件 | 支持收发 | 按 IP Messenger 附件清单解析；文件大小、消息包号和文件 ID 使用协议规定的进制编码；接收时兼容发送端 UDP 源端口并回退标准端口 |
| 文件预览 | 支持 | 聊天消息、右侧接收文件列表均可使用 macOS Quick Look 预览 |
| 剪贴板 | 支持 | 文本按光标位置粘贴；PNG、TIFF、JPEG、HEIC 和 Finder 图片文件可粘贴为待发送图片 |
| 文件夹、语音 | 暂不支持 | 普通文件已支持；文件夹需要 GETDIRFILES 层级流，语音暂按普通文件处理 |

### 表情与抖动兼容边界

表情码参考 [feiq-android 的 FeiqEmoticons.kt](https://github.com/zyqg/feiq-android/blob/8314e26cd043e3f83f0fb69b48653d1eb7504203/android/app/src/main/java/com/feiq/droid/net/FeiqEmoticons.kt)，抖动报文依据仓库内的 `feiqiu-README.md`。系统表情面板或粘贴输入的任意 Unicode emoji 不保证被飞秋 2013 正确显示，跨端交流请优先使用应用内“飞秋表情”面板。未映射的飞秋码不主动删除；若仍出现符号码，请提供原始正文以补充对应客户端的变体。

抖一抖仅用于在线联系人的点对点会话，不中继给群成员。发送提示表示本地已发起请求，不代表对方已显示动画；可在网络日志中查看抖动确认。UDP 丢包、对方关闭窗口抖动或客户端实现差异都可能影响实际效果。

表情与抖动回归检查位于 `Tests/InlineImageProtocolChecks.swift`，覆盖 96 项表情编解码、连续表情、字体标记、变体处理、中文混排编码，以及抖动请求和回执的精确字节格式。

远程协助请求的命令识别也在该回归检查中覆盖。当前仅依据真实日志确认 `0xB0` 请求，飞秋后续的控制/画面传输通道和同意回包尚未有公开协议定义；需要继续抓取“发送请求、接受、拒绝、建立连接、结束协助”完整链路后，才能实现真正的远程控制互通。

### 群聊兼容边界

原生建群、邀请及接受/拒绝邀请尚未实现。协议前置条件、抓包清单和验收标准见 [原生群聊实施说明](GROUP_INTEROP.md)。现有中继会转发群成员发来的普通私聊，不能区分私聊与群消息，请勿用于敏感私聊。

飞秋 2013 Windows 版没有可供本项目直接使用的原生群聊报文，因此当前采用“Mac 中继群聊”方案：Mac 必须保持在线，成员之间的消息先发送到 Mac，再由 Mac 转发给其他群成员。Windows 端收到的底层仍是普通飞秋点对点消息，但消息正文带有群名和原始发送人标记，因此所有在线成员可以看到同一群聊内容。

由于传统飞秋消息本身不携带群组 ID，群成员发给 Mac 的普通消息会被视为群消息并转发给其所在群组的其他成员。若需要私聊，请使用单聊会话。Mac 下线后，群聊中继会暂时停止，成员之间不会自动直连。

## 技术架构

项目遵循单向依赖原则：

```text
View → ViewModel → Repository → Service → Model
```

- `View`：只负责界面展示、用户交互和局部 UI 状态，不直接访问网络或数据库。
- `ViewModel`：管理界面可观察状态、会话切换、未读数、加载状态和动画选择，不直接操作底层存储或 Socket。
- `Repository`：组合网络、历史记录和通知服务，完成业务编排、数据转换、会话归并和消息路由。
- `Service`：提供网络、SQLite、UserDefaults 和 macOS 通知等底层能力，不承载页面业务逻辑。
- `Model`：定义联系人、消息、群聊、身份信息及飞秋协议报文等领域数据结构。

### 目录结构

```text
FeiQMac/
├── Models/          # 联系人、消息、群聊、身份和领域模型
├── Protocol/        # 飞秋报文格式、命令码、编码和表情转换
├── Network/         # UDP/TCP Socket 和网络生命周期管理
├── Persistence/     # SQLite 数据库、表结构、分页查询和旧数据迁移
├── Services/        # 历史记录、通知和用户设置等底层服务
├── Repositories/    # 网络、存储和通知的业务组合层
├── ViewModels/      # SwiftUI 页面状态和用户交互逻辑
├── Views/           # 主界面、侧栏、聊天页、设置、群聊和公共组件
└── Resources/       # Info.plist 和应用资源
```

## 环境要求

- macOS 14.0+
- Xcode 15 或更高版本
- Swift 5
- 运行设备与 Windows 飞秋客户端位于同一个局域网广播域

## 构建与运行

### 使用 Xcode

1. 使用 Xcode 打开 `FeiQMac.xcodeproj`。
2. 选择 `FeiQMac` Scheme。
3. 选择本机 Mac 作为运行目标并启动。

### 使用命令行

```sh
xcodebuild \
  -project FeiQMac.xcodeproj \
  -scheme FeiQMac \
  -configuration Debug \
  -sdk macosx \
  CODE_SIGNING_ALLOWED=NO \
  build
```

建议启动构建生成的 `.app`。不要直接运行 `Contents/MacOS/FeiQMac`，否则 macOS 可能无法按照应用身份正确处理本地网络权限和通知能力。

## 首次运行配置

首次启动时需要在 macOS 中允许以下权限：

1. 打开“系统设置 → 隐私与安全性 → 本地网络”，允许“飞秋 Mac”访问本地网络。
2. 如果系统防火墙拦截入站连接，在“系统设置 → 网络 → 防火墙 → 选项”中允许“飞秋 Mac”接收传入连接。
3. 如需桌面提醒，在通知设置中允许“飞秋 Mac”发送通知。
4. 首次使用截屏功能时，按 macOS 提示允许“屏幕录制”或截图相关权限。

应用设置页可以修改昵称、主机名、分组以及聊天记录进入动画。保存身份信息后，应用会重新广播上线信息。

## 联调说明

### Windows 端看不到 Mac

- 确认两台设备连接到同一个局域网和同一个广播域。
- 确认 macOS 已授权本地网络访问。
- 确认 Windows 防火墙放行 UDP/TCP `2425`。
- 检查网络日志中是否出现“已监听 UDP/TCP 2425，正在广播上线信息”。
- 部分企业 Wi-Fi、访客网络或 VLAN 会隔离广播，此时即使 IP 可达，也可能无法发现用户。

### 中文显示乱码

飞秋 2013 Windows 版通常按 GB18030/GBK 解析传统消息报文。项目对发送给旧版 Windows 的消息使用兼容编码；如果对方是修改版客户端，仍可能因其编码策略不同而出现差异。请优先使用网络日志确认实际收发路径和报文解析结果。

### 消息发送失败

发送失败通常与以下因素有关：

- 对方已经下线或 IP 地址发生变化；
- UDP `2425` 被防火墙或网络隔离策略拦截；
- 两台设备不在同一广播域；
- 对方客户端未监听标准飞秋端口；
- 目标客户端对飞秋扩展版本或编码存在兼容差异。

### 聊天记录位置

默认存储路径为：

```text
~/Documents/飞秋 Mac/ChatHistory.sqlite
```

图片附件默认存储在：

```text
~/Documents/飞秋 Mac/Images
```

普通文件附件默认存储在：

```text
~/Documents/飞秋 Mac/Files
```

同目录下的 `ChatHistory.json` 用于旧版本数据迁移。数据库文件属于本地用户数据，删除或移动前请先退出应用并做好备份。

### 图片收发失败

- 确认 Windows 飞秋和 Mac 位于同一局域网，并且双方防火墙放行 TCP/UDP `2425`。
- 飞秋“粘贴图片/截图”走 UDP 分片；以“文件”发送的图片才走 TCP 下载。日志中的 `0x2000C0` 是图片数据，`0xC1` 是分片确认。
- 正文中 `/~#>八位ID<B~` 与图片分片关联，分片可以乱序或重复到达。接收超时后显示失败提示，不再把图片 ID 当作聊天文字。
- 发送图片统一转换为 JPEG，最长边限制为 4096 像素、质量 0.9；GIF 发送静态首帧，透明度不保留。单张源文件/传输图片上限为 20 MB。
- 接收支持 JPEG 等可由 ImageIO 解码的图像及飞秋未压缩 DIB 位图；不支持的压缩变体会记录解码错误。
- 当前只处理常见图片扩展名：`jpg`、`jpeg`、`png`、`gif`、`bmp`、`tif`、`tiff`、`webp`、`heic`、`heif`。
- 飞秋不同版本的文件附件实现可能存在差异，若无法接收，请保留应用“网络日志”中的附件元数据和 TCP 错误信息用于兼容性排查。
- 剪贴板图片会先转换为 JPEG 保存到 `~/Documents/飞秋 Mac/Images`，点击发送后才进入飞秋内嵌图片分片流程；转换期间发送按钮会暂时禁用。

旧版本只存下图片占位码、没有保存图片字节的历史消息无法自动还原，需要对方重新发送。

图片接收会先展示占位状态，随后更新同一条消息及其持久化附件，不重复增加未读数。90 秒未完成时提示，当前进程继续保留最长 10 分钟关联以补全晚到的图片；最终超时后需要对方重发，不支持重启后续传。分片被接纳后立即回 ACK，再执行完整图像拼接，并按协议参考补发关键 ACK。JPEG/PNG/BMP 与原始 DIB 按内容识别，避免位图标志不一致导致拒收；多附件下载只部分成功时仍保留成功图片，并提示失败数量。

接收流程回归测试位于 `Tests/IncomingImageRepositoryChecks.swift`，使用模拟网络和临时 SQLite，覆盖占位、超时、晚到补全、相反到达顺序、失败提示和重新打开数据库读取；未代替 Windows 双机验证。

协议回归检查位于 `Tests/InlineImageProtocolChecks.swift`，覆盖 Windows 报文格式、二进制尾部、乱序重组、去重、跨好友 ID 隔离和丢包重试。协议依据参考 [HIM](https://github.com/alakenda/HIM) 的截图协议及 [feiq-android](https://github.com/zyqg/feiq-android) 的抓包说明；实际 Windows 飞秋 2013 显示效果仍需双机联调验证。

### 普通文件收发与预览

- 在聊天输入区点击回形针，选择一个或多个普通文件；文件会先复制到 `~/Documents/飞秋 Mac/Files`，点击“发送”后才发出。
- 普通文件通知使用 `IPMSG_FILEATTACHOPT`，对方随后通过 TCP `2425` 发送 `GETFILEDATA` 请求；请求中的消息包号、文件 ID 和偏移量按十六进制解析。
- 接收文件会先创建安全的本地目标路径，再以流式方式写入，不把整个文件读入内存；会优先尝试发送端 UDP 源端口，失败后回退 TCP `2425` 并自动重试；单个文件上限为 2 GB。
- 文件消息只在消息区域展示文件卡片，不套用文字气泡。点击卡片即可打开 Quick Look；不支持预览的格式可以点击“打开”交给默认应用处理。
- 接收文件列表位于单聊右侧资料栏，历史记录重新加载后仍可预览已保存的文件。
- 文件夹附件暂不下载；若日志出现 `No route to host` 或 `Connection refused`，请在 Windows 防火墙中允许飞秋程序接收 TCP `2425` 入站连接，并保留网络日志中的附件清单、消息包号和 TCP 错误信息。

## 数据与隐私

- 应用不依赖中心服务器，消息默认只通过局域网 UDP 直接发送。
- 聊天记录保存在本机“文稿/飞秋 Mac”目录，不会自动上传云端。
- 当前飞秋兼容报文未提供端到端加密，局域网内具备抓包能力的设备可能读取消息内容。
- 请不要在未确认网络可信的环境中传输敏感信息。

## 后续规划

- 实现 GETDIRFILES 文件夹层级传输，并完善不同版本的 TCP 文件传输兼容性。
- 扩展更多飞秋 Windows 表情码和富文本兼容能力。
- 增加历史消息搜索、导出和数据库维护工具。
- 继续完善不同飞秋版本、改版客户端和复杂网络环境下的兼容性测试。
- 评估更完整的跨客户端群聊协议支持。

## 许可证与免责声明

本项目为独立开发的 macOS 客户端实现，与飞秋及其相关商业主体不存在官方隶属或授权关系。飞秋名称、商标及 Windows 客户端的相关权利归其各自权利人所有。

当前仓库未声明开源许可证。若要公开分发或允许第三方基于本项目开发，请先补充明确的许可证和贡献规范。
